## 1.coreuntils

### 1.1 文件管理

```
cat	：连接文件并打印到标准输出设备上。
例：
	cat -n dir.txt
参数说明：
	-n：由 1 开始对所有输出的行数编号。
	-b：和 -n 相似，只不过对于空白行不编号。
	-s：当遇到有连续两行以上的空白行，就代换为一行的空白行。
	-v：使用 ^ 和 M- 符号，除了 LFD 和 TAB 之外。
	-E: 在每行结束处显示 $。
	-T: 将 TAB 字符显示为^I。
```

```
cut：截取子串进行输出
例：
	cat test |cut -c 3	（截取每一行的第三个字符进行输出）
参数说明：
	-b ：以字节为单位进行分割。这些字节位置将忽略多字节字符边界，除非也指定了 -n 标志。
    -c ：以字符为单位进行分割。
    -d ：自定义分隔符，默认为制表符。
    -f ：与-d一起使用，指定显示哪个区域。
    -n ：取消分割多字节字符。仅和 -b 标志一起使用。
```

```
chmod：对文件的权限进行修改
例：
	chmod ugo+rx dir.txt
参数说明：
	u 表示该文件的拥有者；g 表示与该文件的拥有者属于同一个群体(group)者；o 表示其他以外的人；a 表示这三者皆是。
	r 表示可读取；w 表示可写入；x 表示可执行；X 表示只有当该文件是个子目录或者该文件已经被设定过为可执行。
```

```
chown：设置文件所有者和文件关联组的命令
（注：必须root权限才能执行）
例：
	chown -c wanghao:wang test.txt	:将test.txt的所有者设置为wanghao，群体使用者设置为wang。
参数说明：
	-c : 显示更改的部分的信息
    -f : 忽略错误信息
    -h :修复符号链接
    -v : 显示详细的处理信息
    -R : 处理指定目录以及其子目录下的所有文件
```

```
chcon:修改对象（文件）的安全上下文
例：
	chcon -R -t public_content_t /var/ftp 	：把ftp共享给匿名用户
参数说明：
	-h	影响符号连接而非引用的文件
    -R	递归处理所有的文件及子目录
    -v	为处理的所有文件显示诊断信息
    -u	设置指定用户的目标安全环境
    -r	设置指定角色的目标安全环境
    -t	设置指定类型的目标安全环境
    -l	设置指定范围的目标安全环境
```

```
chgrp:于变更文件或目录的所属群组。
例：
	chgrp -v wang test1.txt 
参数说明：
	-c ：效果类似"-v"参数，但仅回报更改的部分。
    -f ： 　不显示错误信息。
    -h ： 　只对符号连接的文件作修改，而不改动其他任何相关文件。
    -R ： 　递归处理，将指定目录下的所有文件及子目录一并处理。
    -v ： 　显示指令执行过程。
```

```
chroot:改变根目录。
例：
	chroot /mnt/ls
参数说明：
	--help 　在线帮助。
	--version 　显示版本信息。
```

```
cksum:检查文件的CRC是否正确
例：
	cksum testfile1
参数说明：
	--help：在线帮助。
	--version：显示版本信息。
```

```
cmp:比较两个文件是否有差异。
例：
	cmp -c file1 file2
参数说明：
	-c或--print-chars：除了标明差异处的十进制字码之外，一并显示该字符所对应字符。
	-i<字符数目>或--ignore-initial=<字符数目>：指定一个数目。
	-l：标示出所有不一样的地方。
	-s：不显示错误信息。
```

```
file:辨识文件类型。
例：
	file -b install.log  
参数说明：
	-b：列出辨识结果时，不显示文件名称。
    -c：详细显示指令执行过程，便于排错或分析程序执行的情形。
    -f<名称文件>：指定名称文件，其内容有一个或多个文件名称时，让file依序辨识这些文件，格式为每列一个文件名称。
    -L：直接显示符号连接所指向的文件的类别。
    -m<魔法数字文件>：指定魔法数字文件。
    -v：显示版本信息。
    -z：尝试去解读压缩文件的内容。
```

```
ln：为某一个文件在另外一个位置建立一个同步的链接。
例：
	ln -s test.txt link_1
参数说明：
	--backup[=CONTROL]：备份已存在的目标文件
    -b 类似 --backup，但不接受参数
    -d：允许超级用户制作目录的硬链接
    -f：强制执行
    -i：交互模式，文件存在则提示用户是否覆盖
    -n：把符号链接视为一般目录
    -s：软链接(符号链接)
    -v：显示详细的处理过程
```

```
locate：查找符合条件的文档
（注：若对及时更新的文件进行查找，需用管理员权限使用updatedb命令进行更新后再进行查找）
例：
	locate mytest.txt
参数说明：
	-b -- 仅匹配路径名的基本名称
    -c -- 只输出找到的数量
    -d -- 使用 DBPATH 指定的数据库，而不是默认数据库 /var/lib/mlocate/mlocate.db
    -e -- 仅打印当前现有文件的条目
    -n -- 至多显示 n个输出。
    -i -- 忽略大小写
```

```
mv：为文件或目录改名、或将文件或目录移入其它位置。
例：
	mv test.txt ..
参数说明：
	-b: 当目标文件或目录存在时，在执行覆盖前，会为其创建一个备份。
    -i: 如果指定移动的源目录或文件与目标的目录或文件同名，则会先询问是否覆盖旧文件，输入 y 表示直接覆盖，输入 n 表示取消该操作。
    -f: 如果指定移动的源目录或文件与目标的目录或文件同名，不会询问，直接覆盖旧文件。
    -n: 不要覆盖任何已存在的文件或目录。
    -u：当源文件比目标文件新或者目标文件不存在时，才执行移动操作。
```

```
od：以八进制字码输出文件内容。
例：
	od -b tmp
参数说明：
	-A<字码基数>：选择要以何种基数计算字码。
	-j<字符数目>：略过设置的字符数目。
	-N<字符数目>：到设置的字符数目为止。
	-s<字符串字符数>：只显示符合指定的字符数目的字符串。
    -t<输出格式>：设置输出格式。
    -v：输出时不省略重复的数据。
    -w<每列字符数>：设置每列的最大字符数。
```

```
rm：删除一个文件或者目录。
例：
	rm  test.txt 
参数说明：
	-i：删除前逐一询问确认。 
    -f：即使原档案属性设为唯读，亦直接删除，无需逐一确认。
    -r：将目录及以下之档案亦逐一删除。
```

```
tee：分离输出
例：
	date|tee hello.list	:直接覆盖原文件，若没有该文件则创建
	ls -c|tee -a hello.list	:再原文件后进行追加操作，若没有该文件则创建
参数说明：
	-a：附加到既有文件的后面，而非覆盖它．
	-i：忽略中断信号。
```

```
touch：修改或创建文件或者目录的时间属性
例：
	touch newfile
参数说明：
	a 改变档案的读取时间记录。
    m 改变档案的修改时间记录。
    c 假如目的档案不存在，不会建立新的档案。与 --no-create 的效果一样。
    f 不使用，是为了与其他 unix 系统的相容性而保留。
    r 使用参考档的时间记录，与 --file 的效果一样。
    d 设定时间与日期，可以使用各种不同的格式。
    t 设定档案的时间记录，格式与 date 指令相同。
```

```
cp：复制文件或目录。
例：
	cp -r file1 ./file2
参数说明：
	-a：此选项通常在复制目录时使用，它保留链接、文件属性，并复制目录下的所有内容。其作用等于dpR参数组合。
    -d：复制时保留链接。这里所说的链接相当于 Windows 系统中的快捷方式。
    -f：覆盖已经存在的目标文件而不给出提示。
    -i：与 -f 选项相反，在覆盖目标文件之前给出提示，要求用户确认是否覆盖，回答 y 时目标文件将被覆盖。
    -p：除复制文件的内容外，还把修改时间和访问权限也复制到新文件中。
    -r：若给出的源文件是一个目录文件，此时将复制该目录下所有的子目录和文件。
    -l：不复制文件，只是生成链接文件。
```

```
read：标准输入读取数值。
例：
	read input1
	echo $input1
参数说明：
	-a 后跟一个变量，该变量会被认为是个数组，然后给其赋值，默认是以空格为分割符。
    -d 后面跟一个标志符，其实只有其后的第一个字符有用，作为结束的标志。
    -p 后面跟提示信息，即在输入前打印提示信息。
    -e 在输入的时候可以使用命令补全功能。
    -n 后跟一个数字，定义输入文本的长度，很实用。
    -r 屏蔽\，如果没有该选项，则\作为一个转义字符，有的话 \就是个正常的字符了。
    -s 安静模式，在输入字符时不再屏幕上显示，例如login时输入密码。
    -t 后面跟秒数，定义输入字符的等待时间。
    -u 后面跟fd，从文件描述符中读入，该文件描述符可以是exec新开启的。
```

```
dirname：去除文件名中的非目录部分，仅显示与目录有关的内容
例：
	 dirname /home/wanghao/study_source/test/test	
参数说明：
	-z：用空字符 NUL 而不是换行符分隔输出
```

```
head：查看文件的开头部分的内容
head [参数] [文件]  
例：
	head -n 10 test.txt
参数说明：
	-q 隐藏文件名
    -v 显示文件名
    -c<数目> 显示的字节数。
    -n<行数> 显示的行数。
```

```
md5sum：计算和校验文件的MD5值
md5sum [选项] 文件
例：
	md5sum test.txt
参数说明：
	-b	以二进制模式读取文件
    -t	以文本模式读入文件内容
    -c	根据已生成的md5值，对现存文件进行校验
    -w	检查输入的md5信息有没有非法行，若有则输出相应信息
    –status	校验完成后，不生成错误或正确的提示信息，可以通过命令的返回值来判断
```

```
mktemp:建立暂存文件。
mktemp [-qu][文件名参数]
例：
	mktemp demo
参数说明：
	-q 　执行时若发生错误，不会显示任何信息。
	-u 　暂存文件会在mktemp结束前先行删除。
```

```
nl：计算文件的行号
例：
	nl -b a test.txt
参数说明：
	-b ：指定行号指定的方式，主要有两种：

    -b a ：表示不论是否为空行，也同样列出行号(类似 cat -n)；

    -b t ：如果有空行，空的那一行不要列出行号(默认值)；

    -n ：列出行号表示的方法，主要有三种：

    -n ln ：行号在萤幕的最左方显示；

    -n rn ：行号在自己栏位的最右方显示，且不加 0 ；

    -n rz ：行号在自己栏位的最右方显示，且加 0 ；

    -w ：行号栏位的占用的位数。

    -p 在逻辑定界符处不重新开始计算。
```

```
paste：合并文件的列
paste [-s][-d <间隔字符>][--help][--version][文件...]
例：
	 paste file testfile
参数说明：
	-d<间隔字符>或--delimiters=<间隔字符> 　用指定的间隔字符取代跳格字符。
	-s或--serial 　串列进行而非平行处理。
```

```
pathchk：检查文件中不可移植的部分。
pathchk [参数]
例：
	pathchk test.txt
参数说明：
	-p	检查大多数的POSIX系统
	-P	检查空名字和“-”开头的文件
	--portability	检查所有的POSIX系统，等同于“-P-p”选项	
```

```
pr：指定文件附注打印所需的页码或聚焦
pr(选项)(参数)
例：
	pr -2 /root/install.log	（对/root/install.log文件附注打印所需的页码或聚焦，输出指定2列）
参数说明：
	-h：为页指定标题；
 	-l：指定每页的行数。
```

```
readlink:找出符号链接所指向的位置。
readlink [参数] [文件]
例：
	readlink -f  
参数说明：
	-f	递归跟随给出文件名的所有符号链接以标准化,除最后一个外所有组件必须存在
    -e	递归跟随给出文件名的所有符号链接以标准化，所有组件都必须存在
    -n	不输出尾随的新行
    -s	缩减大多数的错误消息
    -v	报告所有错误消息
```

```
realpath:获取指定目录或文件的绝对路径
realpath [OPTION]... FILE...
例：
	realpath text.txt
参数说明：
	-e, --canonicalize-existing：文件 FILE 的所有组成部件必须都存在
	-m, --canonicalize-missing：文件 FILE 的组成部件可以不存在
	-L, --logical：在软链接之前解析..
	-P, --physical：解析软链接，默认动作
	-q, --quiet：静默模式输出，禁止显示大多数错误消息
```

```
split：分割文件
split [--help][--version][-<行数>][-b <字节>][-C <字节>][-l <行数>][要切割的文件][输出文件名]
例：
	split -6 test.txt	将test文件每六行分割成一个文件
参数说明：
	-<行数> : 指定每多少行切成一个小文件
	-b<字节> : 指定每多少字节切成一个小文件
```

```
sum：计算文件的校验码和显示块数
sum(选项)(参数)
例：
	sum text.txt
参数说明：
	 -r：使用BSD的校验和算法，块大小为1k；
     -s：使用system V的校验和算法，块大小为512字节。
```

```
tail：查看文件的内容
tail [参数] [文件]  
例：
	tail my.log	(默认显示最后10行)
参数说明：
	-f 循环读取
    -q 不显示处理信息
    -v 显示详细的处理信息
    -c<数目> 显示的字节数
    -n<行数> 显示文件的尾部 n 行内容
    --pid=PID 与-f合用,表示在进程ID,PID死掉之后结束
    -q, --quiet, --silent 从不输出给出文件名的首部
    -s, --sleep-interval=S 与-f合用,表示在每次反复的间隔休眠S秒
```

```
tac：将文件以行为单位的反序输出（按行）
tac [OPTION]... [FILE]...
例：
	tac test.txt
参数说明：
    -b, --before：在行前而非行尾添加分隔标志
    -r, --regex：将分隔标志视作正则表达式来解析
    -s, --separator=STRING：指定字符串代替换行符作为行分隔标志
```

```
unlink：删除单个文件的命令行实用程序
unlink filename	（注：一旦删除文件，就无法完全恢复文件）
```

```
uniq：检查及删除文本文件中重复出现的行列
uniq [-cdu][-f<栏位>][-s<字符位置>][-w<字符位置>][--help][--version][输入文件][输出文件]
例：
	uniq testfile （删除文件中重复的行）
参数说明：
	-c或--count 在每列旁边显示该行重复出现的次数。
    -d或--repeated 仅显示重复出现的行列。
    -f<栏位>或--skip-fields=<栏位> 忽略比较指定的栏位。
    -s<字符位置>或--skip-chars=<字符位置> 忽略比较指定的字符。
    -u或--unique 仅显示出一次的行列。
    -w<字符位置>或--check-chars=<字符位置> 指定要比较的字符。
```

```
truncate：将文件缩小或扩展到指定的大小
例：
	truncate -s 0 yum.log （可用于清除文件内容）
参数说明：
	-c, --no-create --> 不创建任何文件
    -o, --io-blocks --> 将大小视为存储块的数量，而不是字节
    -r, --reference=RFILE --> 参考指定的文件大小
    -s, --size=SIZE --> 按照指定的字节设置文件大小
```

### 1.2 文档编辑

```
egrep:在文件内查找指定的字符串。
例：
	egrep test * （在当前目录查找文件中含有test子串的文件，并罗列出来）
语法：
	egrep [范本模式] [文件或目录] 
    [范本模式] ：查找的字符串规则。
    [文件或目录] ：查找的目标文件或目录。
```

```
grep：查找文件里符合条件的字符串或正则表达式。
例：
	ls -l|grep "test.*"
	grep "hello" test.txt (找到则返回存在hello的哪一行)
参数说明：
	-i：忽略大小写进行匹配。
    -v：反向查找，只打印不匹配的行。
    -n：显示匹配行的行号。
    -r：递归查找子目录中的文件。
    -l：只打印匹配的文件名。
    -c：只打印匹配的行数。
```

```
sort：文本文件排序（按行排序）
例：
		sort ls.list
参数说明：
	-b	：忽略前导空格
	-d	：按照字典顺序排序
	-f	：忽略大小写
	-n	：数字按数值排序
	-o	：将输出存储在指定文件 
	-r	：倒序排列
```

```
tr：删除一段信息当中的文字，或者是替换文字信息
例：
	cat ls.list|tr -r "a" "A"	:将ls.list中的a全部替换成A展示
	echo "aaaaaabbbbnnnnn"|tr -d "a"	:将字符串中的a全部删除再进行展示
选项查看：
	-r	：进行文本替换
	-d	：进行指定文本删除
```

```
expr：手工命令行计数器
1.数值计算：
	echo `expr $num1 + $num2`
	echo `expr $num1 * $num2`
	echo `expr $num1 / $num2`
	echo `expr $num1 - $num2`
2.获取字符串长度：
	echo `expr length "$str"`（注：双引号是包含了空格的长度）
	echo `expr length ''$str'`(注：单引号是去重复和去空格后的长度)
3.字符串匹配：
	expr match "${str}" "he.*"(match只能从字符串开头进行匹配，返回匹配字符串长度，可结合正则匹配进行)
	expr index ${str} "wor.*"(index可以匹配中间字符串，返回子串在字符串中最早第一次出现的单个字符的位置)
```

```
wc：计算出输出信息的整体数据
例：
	wc -c ls.txt	：统计ls.txt中的字符数
选项查看：
	-c：统计字符数
	-w：统计单词个数
	-l：报告行数
```

```
comm：用于比较两个已排过序的文件
(注：如果没有指定任何参数，则会把结果分成3列显示：第1列仅是在第1个文件中出现过的列，第2列是仅在第2个文件中出现过的列，第3列则是在第1与第2个文件里都出现过的列)
例：
	comm file1 file2
参数说明：
	-1 不显示只在第 1 个文件里出现过的列。
    -2 不显示只在第 2 个文件里出现过的列。
    -3 不显示只在第 1 和第 2 个文件里出现过的列。
```

```
csplit：分割文件
例：
	csplit test1.txt 2	（从第二行将test1.txt文件分割为两份）
参数说明：
        -b<输出格式>： 预设的输出格式其文件名称为xx00,xx01...等，您可以通过改变<输出格式>来改变输出的文件名。
    -f<输出字首字符串>： 预设的输出字首字符串其文件名为xx00,xx01...等，如果你指定输出字首字符串为"hello"，则输出的文件名称会变成hello00,hello01...等。
    -k： 保留文件，就算发生错误或中断执行，也不能删除已经输出保存的文件。
    -n<输出文件名位数>： 预设的输出文件名位数其文件名称为xx00,xx01...等，如果你指定输出文件名位数为"3"，则输出的文件名称会变成xx000,xx001...等。
    -q： 不显示指令执行过程。
    -z： 删除长度为0 Byte文件。
```

```
expand：用于将文件的制表符（Tab）转换为空格符（Space）
例：
	expand -i -t 6 test.txt	(将test文件中每行第一个tab替换为个空格)
参数说明：
	-i, --initial：转换非空白符后的制表符
    -t, --tabs=NUMBER：定一个tab替换为多少个空格，而不是默认的 8
    -t, --tabs=LIST：定制表符位置列表，用逗号分隔
```

```
fmt：编排文本文件并输出
fmt [-cstu][-p<列起始字符串>][-w<每列字符数>][--help][--version][文件...]
例：
	fmt -u test.txt
参数说明：
	-c或--crown-margin 每段前两列缩排。
    -p<列起始字符串>或-prefix=<列起始字符串> 仅合并含有指定字符串的列，通常运用在程序语言的注解方面。
    -s或--split-only 只拆开字数超出每列字符数的列，但不合并字数不足每列字符数的列。
    -t或--tagged-paragraph 每列前两列缩排，但第1列和第2列的缩排格式不同。
    -u或--uniform-spacing 每个字符之间都以一个空格字符间隔，每个句子之间则两个空格字符分隔。
    -w<每列字符数>或--width=<每列字符数>或-<每列字符数> 设置每列的最大字符数。
```

```
fold：限制文件列宽
fold [-bs][-w<每列行数>][--help][--version][文件...]
例：
	fold -w 20 test.txt
参数说明：
	-b或--bytes 以Byte为单位计算列宽，而非采用行数编号为单位。
    -s或--spaces 以空格字符作为换列点。
    -w<每列行数>或--width<每列行数> 设置每列的最大行数。
```

```
join：将两个文件中，指定栏位内容相同的行连接起来。
join [-i][-a<1或2>][-e<字符串>][-o<格式>][-t<字符>][-v<1或2>][-1<栏位>][-2<栏位>][--help][--version][文件1][文件2]
例：
	file1
    zhang	600
    liu		150
    file2
    zhang	60
    liu		15
  	join file1 file2
  	zhang	600		60
    liu		150		15
参数说明：
	-a<1或2> 除了显示原来的输出内容之外，还显示指令文件中没有相同栏位的行。
    -e<字符串> 若[文件1]与[文件2]中找不到指定的栏位，则在输出中填入选项中的字符串。
    -i或--igore-case 比较栏位内容时，忽略大小写的差异。
    -o<格式> 按照指定的格式来显示结果。
    -t<字符> 使用栏位的分隔字符。
    -v<1或2> 跟-a相同，但是只显示文件中没有相同栏位的行。
    -1<栏位> 连接[文件1]指定的栏位。
    -2<栏位> 连接[文件2]指定的栏位。
```

```
unexpandL:空格字符转换为制表符
unexpand [选项] [文件]
例：
	unexpand -t 4 /root/install.log
参数说明：
	-a：转换所有空格字符而不仅仅是字母首部的空格
	-t<列表>：使用都好分割的列表列出制表符位置
	-t<宽度>：设置制表符宽度而非默认的8
```

### 1.3 系统管理

```
useradd:新增使用者帐号
例：
	useradd wang
参数说明：
	-r 　建立系统帐号。
	-f<缓冲天数> 　指定在密码过期后多少天即关闭该帐号。
	-g<群组> 　指定用户所属的群组。
	-G<群组> 　指定用户所属的附加群组。
	-n 　取消建立以用户名称为名的群组．
```

```
date：显示或设定系统的日期与时间。
例：
	date -R
参数说明：
	-d：通过字符串显示时间格式，字符串不能是'now'。
    -R： 按照 RFC 5322 格式输出时间和日期，例如: Mon, 14 Aug 2006 02:34:56 -0600。
    -r：显示文件的上次修改时间。
    -s：根据字符串设置系统时间。
    -u：显示或设置协调世界时(UTC)。
```

```
sleep：延时执行
sleep 秒数
例：
	sleep 20
```

```
reboot:重新启动计算机。
参数说明：
    -n : 在重开机前不做将记忆体资料写回硬盘的动作
    -w : 并不会真的重开机，只是把记录写到 /var/log/wtmp 档案里
    -d : 不把记录写到 /var/log/wtmp 档案里（-n 这个参数包含了 -d）
    -f : 强迫重开机，不呼叫 shutdown 这个指令
    -i : 在重开机之前先把所有网络相关的装置先停止
```

```
sudo：以系统管理者的身份执行指令
例：
	sudo chmod 777 file1
参数说明：
	-h 会显示版本编号及指令的使用方式说明
    -l 显示出自己（执行 sudo 的使用者）的权限
    -u username/#uid 不加此参数，代表要以 root 的身份执行指令，而加了此参数，可以以 username 的身份执行指令（#uid 为该 username 的使用者号码）
```

```
dircolors：设置 ls 指令在显示目录或文件时所用的色彩
例：
	dircolors -p	（显示默认值）

参数说明：
    -b:显示在Boume shell中，将LS_COLORS设为目前预设置的shell指令。
    -c:显示在C shell中，将LS_COLORS设为目前预设置的shell指令。
    -p:显示预设置
```

```
env：查看或设置当前用户环境变量信息
例：
	env|grep “PATH”		（查看PATH相关的环境变量信息）
参数说明：
	-0：以空字符而非新行符结束每一输出行
	-i：以无定义的环境启动
	-u<名称>：从当前环境中撤销一个变量
```

```
groups：查询用户所在的组
groups [OPTION]... [USERNAME]...
例：
	groups root	（查询root所在的组）
```

```
hostid:显示当前主机的十六进制数字标识。
hostid [OPTION]...
 例：
 	hostid
```

```
hostname:显示和设置系统的主机名称（设置的话需要root权限）
（注：使用该命令设置主机名后，系统并不会永久保存新的主机名，重新启动机器之后还是原来的主机名。如果需要永久修改主机名，建议使用hostnamectl）
例：
	sudo hostname new_name	(将主机民临时修改未new_name)
	hostname			(查看主机名)
```

```
id：显示用户的ID，以及所属群组的ID
id [-gGnru][--help][--version][用户名称]
例：
	id -g
参数说明：
	-g 或 --group 　显示用户所属群组的ID。
    -G 或 --groups 　显示用户所属附加群组的ID。
    -n 或 --name 　显示用户，所属群组或附加群组的名称。
    -r 或 --real 　显示实际ID。
    -u 或 --user 　显示用户ID。
```

```
logname：用于显示用户名称
例：
	logname
```

```
mknod：创建字符设备文件和块设备文件
mknod [OPTIONS]   [MAJOR] [MINOR] ...
例：
	 sudo mknod -m 444 /tmp/pipe-with-permissions p
参数说明：
	-Z：设置安全的上下文;
	-m：设置权限模式;
```

```
nice：设置程序运行时的优先级
nice [-n adjustment] [-adjustment] [--adjustment=adjustment] [--help] [--version] [command [arg...]]
例：
	nice -n 1 ls	（将 ls 的优先序加 1 并执行）
参数说明：
	-n adjustment, -adjustment, --adjustment=adjustment 皆为将该原有优先序的增加 adjustment
```

```
pinky：查询登录用户信息
例：
	pinky -f
参数说明：
	-f：避免打印表头
	-w：输出登录用户信息不包括name列
	-i：输出登录用户信息但不包括name和where列
	-q：输出登录用户信息但不包括name、idle和where列
	-l：获取用户的完整详细信息
	-l -b：输出详细信息但不包括目录和shell
	-l -h -p：输出详细信息但不包括计划和项目文件行
```

```
printenv:显示指定的环境变量的值
printenv [参数] [变量]
例：
	printenv	（显示所有环境变量）
参数说明：
	--null	以空字符而非新行符结束每一输出行
```

```
who：显示系统当前使用者
who - [husfV] [user]
例：
	who -q
参数说明：
	-H 或 --heading：显示各栏位的标题信息列；
    -i 或 -u 或 --idle：显示闲置时间，若该用户在前一分钟之内有进行任何动作，将标示成"."号，如果该用户已超过24小时没有任何动作，则标示出"old"字符串；
    -m：此参数的效果和指定"am i"字符串相同；
    -q 或--count：只显示登入系统的帐号名称和总人数；
    -s：此参数将忽略不予处理，仅负责解决who指令其他版本的兼容性问题；
    -w 或-T或--mesg或--message或--writable：显示用户的信息状态栏；
```

```
whoami:显示自身用户名称。
whoami [--help][--version]
```

```
uptime：显示系统总共运行了多长时间和系统的平均负载。
uptime ［options］
例：
	uptime
参数说明：
-p， --pretty 采用可读友好的格式输出系统已运行时长 
-h， --help 显示帮助信息 
-s， --since 以格式 yyyy-mm-dd HHSS format 输出系统启动时间 
-V， --version 显示版本信息
```

```
users：打印当前主机所有登陆用户的名称。
users [OPTION]... [FILE]
例：
	users
参数说明：
	--help       显示帮助信息并退出。
 	--version    显示版本信息并退出。
```

```
uname：显示电脑以及操作系统的相关信息
uname [-amnrsv][--help][--version]
例：
	uname -a
参数说明：
	-a 或--all 　显示全部的信息，包括内核名称、主机名、操作系统版本、处理器类型和硬件架构等。。
    -m 或--machine 　显示处理器类型。
    -n 或--nodename 　显示主机名。
    -r 或--release 　显示内核版本号。
    -s 或--sysname 　显示操作系统名称。
```

### 1.4 进程管理

```
ps：查询进程信息
    ps -a   :显示所有活动进程信息
    ps -f   :显示信息的完整列表
```

```
kill：终止进程
	kill 进程id：		终止进程
	kill -9 进程id：	强行终止进程（用于kill不生效的情况下）
```

```
nohup：在系统后台不挂断地运行命令
nohup Command [ Arg … ] [　& ]
例：
	nohup sh test.sh &		(在后台执行脚本)
参数说明：
	a : 显示所有程序
    u : 以用户为主的格式来显示
    x : 显示所有程序，不区分终端机
```

```
nproc：显示当前进程可用的CPU数目
nproc [OPTION]...
例：
	nproc
参数说明：
	 --all         打印已安装处理器的数量。
 	--ignore=N    如果可以的情况下，排除 N 个处理单元。
```

```
timeout：控制程序运行的时间
例：
	timeout 10 ping www.baidu.com	(10s之后，终止ping操作)
参数说明：
	s : 秒 (默认)
	m : 分钟
 	h : 小时
 	d : 天
	-s : --signal=信号，指定在超时时发送的信号。信号可以是类似"HUP"的信号名或是信号数。，查看"kill -l"以获得信号列表
```

### 1.5 磁盘管理

```
dd：读取、转换并输出数据。(用于数据备份与恢复)
例：
	dd if=test.txt of=temp conv=ucase	(将test中的小写字母全部转化成大写并输出到temp文件)
	dd if=test of=temp ibs=3 skip=1		（将块大小设置为3字节，从文件头跳过一个块将内容输出值temp）

参数说明：
	if=文件名：输入文件名，默认为标准输入。即指定源文件。
    of=文件名：输出文件名，默认为标准输出。即指定目的文件。
    ibs=bytes：一次读入bytes个字节，即指定一个块大小为bytes个字节。
    obs=bytes：一次输出bytes个字节，即指定一个块大小为bytes个字节。
    bs=bytes：同时设置读入/输出的块大小为bytes个字节。
    cbs=bytes：一次转换bytes个字节，即指定转换缓冲区大小。
    skip=blocks：从输入文件开头跳过blocks个块后再开始复制。
    seek=blocks：从输出文件开头跳过blocks个块后再开始复制。
    count=blocks：仅拷贝blocks个块，块大小等于ibs指定的字节数。
    conv=<关键字>，关键字可以有以下11种：
    conversion：用指定的参数转换文件。
    ascii：转换ebcdic为ascii
    ebcdic：转换ascii为ebcdic
    ibm：转换ascii为alternate ebcdic
    block：把每一行转换为长度为cbs，不足部分用空格填充
    unblock：使每一行的长度都为cbs，不足部分用空格填充
    lcase：把大写字符转换为小写字符
    ucase：把小写字符转换为大写字符
    swap：交换输入的每对字节
    noerror：出错时不停止
    notrunc：不截短输出文件
    sync：将每个输入块填充到ibs个字节，不足部分用空（NUL）字符补齐。
```

```
df：显示目前在 Linux 系统上的文件系统磁盘使用情况统计
例：
	df -h
参数说明：
	文件-a：包含所有的具有 0 Blocks 的文件系统
    文件-h：使用人类可读的格式
    文件-i：列出 inode 资讯，不列出已使用 block
    文件-l：限制列出的文件结构
    文件--no-sync 取得资讯前不 sync (预设值)
    文件-P：使用 POSIX 输出格式
    文件-t：限制列出文件系统的 TYPE
    文件-T：显示文件系统的形式
    文件-x：限制列出文件系统不要显示 TYPE
```

```
du：显示目录或文件的大小。
例：
	du test.txt
参数说明：
	-a或-all：显示目录中个别文件的大小。
    -b或-bytes:显示目录或文件大小时，以byte为单位。
    -c或--total:除了显示个别目录或文件的大小外，同时也显示所有目录或文件的总和。
    -D或--dereference-args:显示指定符号连接的源文件大小。
    -h或--human-readable:以K，M，G为单位，提高信息的可读性。
    -H或--si 与-h:参数相同，但是K，M，G是以1000为换算单位。
    -k或--kilobytes:以1024 bytes为单位。
    -l或--count-links:重复计算硬件连接的文件。
    -L<符号连接>或--dereference<符号连接>：显示选项中所指定符号连接的源文件大小。
    -m或--megabytes：以1MB为单位。
    -s或--summarize：仅显示总计。
    -S或--separate-dirs：显示个别目录的大小时，并不含其子目录的大小。
    -x或--one-file-xystem：以一开始处理时的文件系统为准，若遇上其它不同的文件系统目录则略过。
    -X<文件>或--exclude-from=<文件>：在<文件>指定目录或文件。
```

```
mkdir:创建目录。
mkdir [-p] dirName
例：
	mkdir newlist
参数说明
	-p 确保目录名称存在，不存在的就建一个。
```

```
rmdir:删除空的目录。
rmdir [-p] dirName
例：
	rmdir study_source
参数说明：
	-p 是当子目录被删除后使它也成为空目录的话，则顺便一并删除。
```

```
pwd：命令用于显示工作目录。
pwd [--help][--version]
例：
	pwd
参数说明：
	--help 在线帮助。
	--version 显示版本信息。
```

```
shred：销毁数据操作
例：
	shred -u test.txt （覆盖并删除文件）
参数说明：
	-f, --force 必要时修改权限以使目标可写
    -n, --iterations=N 覆盖N 次，而非使用默认的3 次
    –random-source=文件 从指定文件中取出随机字节
    -s, --size=N 粉碎数据为指定字节的碎片(可使用K、M 和G 作为单位)
    -u, --remove 覆盖后截断并删除文件
    -v, --verbose 显示详细信息
    -x, --exact 不将文件大小增加至最接近的块大小
    -z, --zero 最后一次使用0 进行覆盖以隐藏覆盖动作
```

```
stat：显示 inode 内容。
stat [文件或目录]
```

```
sync：用于数据同步
Linux 系统中欲写入硬盘的资料有的时候为了效率起见，会写到 filesystem buffer中，这个 buffer 是一块记忆体空间，如果欲写入硬盘的资料存于此 buffer中，而系统又突然断电的话，那么资料就会流失了，sync 指令会将存于buffer中的资料强制写入硬盘中。
```

```
ls：显示指定工作目录下之内容
 ls [-alrtAFR] [name...]
 例：
 	ls -l
 参数说明：
 	-a 显示所有文件及目录 (. 开头的隐藏文件也会列出)
    -d 只列出目录（不递归列出目录内的文件）。
    -l 以长格式显示文件和目录信息，包括权限、所有者、大小、创建时间等。
    -r 倒序显示文件和目录。
    -t 将按照修改时间排序，最新的文件在最前面。
    -A 同 -a ，但不列出 "." (目前目录) 及 ".." (父目录)
    -F 在列出的文件名称后加一符号；例如可执行档则加 "*", 目录则加 "/"
    -R 递归显示目录中的所有文件和子目录。
```

```
vidr：等同与ls-l
```

### 1.6 其他

```
echo：写内容到标准输出。
1.echo 字符串常量：直接输出字符串
例：
	echo Hello world
	echo “Hello world”
2.echo $变量：输出变量的值
例：
	age=32  	（注：=赋值不能有空格）
	echo $age
3. echo `执行命令`：输出执行命令后的结果（注：这个不是单引号，是中断音）
例：
	echo ·date·
	echo ·ls -c·
参数说明：
	 -n：不加换行符。
     -e：启用对后续反斜杠转义。
     -E：明确禁止反斜杠转义。
```

```
factor：用于分解因数
例：
	factor 60	（输出为：60: 2 2 3 5）
```

```
false：返回状态为失败。
true：返回状态为成功。
```

```
install：安装或升级软件或备份数据
例：
	install study_source/test/test.txt study_source/temp 
参数说明：
	--backup[=CONTROL]：为每个已存在的目的地文件进行备份。
    -b：类似 --backup，但不接受任何参数。
    -c：(此选项不作处理)。
    -d，--directory：所有参数都作为目录处理，而且会创建指定目录的所有主目录。
    -D：创建<目的地>前的所有主目录，然后将<来源>复制至 <目的地>；在第一种使用格式中有用。
    -g，--group=组：自行设定所属组，而不是进程目前的所属组。
    -m，--mode=模式：自行设定权限模式 (像chmod)，而不是rwxr-xr-x。
    -o，--owner=所有者：自行设定所有者 (只适用于超级用户)。
    -p，--preserve-timestamps：以<来源>文件的访问/修改时间作为相应的目的地文件的时间属性。
    -s，--strip：用strip命令删除symbol table，只适用于第一及第二种使用格式。
    -S，--suffix=后缀：自行指定备份文件的<后缀>。
    -v，--verbose：处理每个文件/目录时印出名称。
```

```
local:在函数内定义局部变量。
 local [-aAfFgilnrtux] [-p] [name[=value] ...]
 例：
 	local temp=“hello”
 参数说明：
 	name（可选）：变量名或已定义函数名。
	value（可选）：变量的值。
```

```
mkfifo：创建有名管道（Named Pipe）
例：
	mkfifo newpipe
	echo "hello" > newpipe
	cat < newpipe
	rm newpipe
参数说明：
	-m 权限：设置管道的权限，其格式与 chmod 命令相同。
	-Z 文件类型：指定创建管道的 SELinux 上下文。
```

```
numfmt:格式化数字
numfmt [选项] 数字
例：
	numfmt -b 255 (输出255的二进制)
参数说明：
	-b：转化为二进制
	-u：转化为字节单位
	-d：指定自定义的分隔符
	-f：指定小数位数。
```

```
seq:输出序列化的一串整数。
语法：
    #输出从1（默认1）到尾数之间的所有整数
    1. seq + 选项 + 尾数
    #输出从首数开始到尾数结束的所有整数
    2. seq + 选项 + 首数 + 尾数
    #输出从首数开始到尾数结束，每隔一个增量输出一个整数，类似于一个等差数列
    3. seq + 选项 + 首数 + 增量 + 尾数
参数说明：
    1. -f :   使用printf 样式的浮点格式
    1. -s :   使用指定字符串分隔数字,默认是换行符
    2. -w :   在列前添加0 使得宽度相同
```

```
shuf：把输入行按随机顺序输出到标准输出
shuf [OPTION]... [FILE]
shuf -e [OPTION]... [ARG]...
shuf -i LO-HI [OPTION]...
例：
	shuf -i 1-10 -o temp	（生成1-10的序列，并随机写入temp文件）
	shuf /data/test.txt	（将文件内的内容随机打乱输出）
参数说明：
	-e --> --echo：以"字符串"为生成源（不能使用"数字范围"）
    -i --> --input-range=固定数字/数字范围：以"数字"为生成源（格式可以是 1 2 3固定几个数字  1-n范围数字）
    -n --> ----head-count=数量：将生成的随机内容写入文件的行数
    -o --> --output=文件路径：将生成的随机内容写入到文件
    --random-source 
    -r --> --repeat：写入文件时内容可重复（注意要和 -n 一起使用限制写入次数，否则无限循环）
    -z --> --zero-terminated：写入文件时，不进行换行
```

```
stty:改变或打印终端行设置。
例：
	stty iuclc	（在命令行下禁止输出大写。）
参数说明：
	-a, --all：以可读的格式打印当前的所有设置。
    -g, --save：以终端可读的格式打印当前的所有设置。
    -F, --file：打开指定的设备，并用此设备作为输入来代替标准输入。
```

```
yes：用于在安装程序或执行命令时自动输入
例：
	yes y|rm *	执行命令时自动输入y
```

```
tty：显示终端机连接标准输入设备的文件名称。
tty [-s][--help][--version]
例：
	tty
参数说明：
	-s或--silent或--quiet 不显示任何信息，只回传状态代码。
```

## 2.Bash基本语法：

### 2.1 变量

```
变量定义：
	变量=值	
变量定义规则：
	   （1）变量名称可以由字母、数字和下划线组成，但是不能以数字开头。
       （2）等号两侧不能有空格
       （3）变量名称一般习惯为大写
```

```
shell特殊变量：
$? 表示上一个命令退出的状态
$$ 表示当前进程编号
$0 表示当前脚本名称
$n 表示n位置的输入参数（n代表数字，n>=1）
$# 表示参数的个数，常用于循环
$*和$@ 都表示包含所有命令行参数
$1… $9：分别包含第一个到第九个命令行参数
```

### 2.2 数值比较

```
-eq:两数相等
-ne:两数不相等
-gt:前者是否大于后者 
-ge:前者是否大于等于后者
-lt:前者是否小于后者
-le:前者是否小于等于后者
```

### 2.2 条件判断结构

```
if-then结构
语法：
	if [ condition ]
	then
		commands
		...
	fi	
```

```
if-then-else结构
语法：
	if [ condition ]
	then
		commands
		...
	else
		commands
		...
	fi
```

### 2.3 循环结构

```
for-in-done 结构
语法：
	for temp in 1 2 3
	do
		commands
		...
	done
```

```
while-do-done 结构
语法：
while [ conditon ]
do
	commands
	...
done
```

```
until-do-done 结构
语法：
	until [ condition ]
	do
		commands
		...
	done
```

## 3.Powershell常用命令：

```
ping ip地址：检查网络是否连接
参数说明：
    ping ip –t   连续对IP地址执行Ping命令,直到被用户以Ctrl+C中断
    ping ip -l  m   指定Ping命令中的数据长度为m字节,缺省为32字节
    ping ip –n    执行特定次数的Ping命令,缺省为4次。
```

```
ipconfig:显示当前的TCP/IP配置的设置值
参数说明：
ipconfig  查看配置
ipconfig /all  查看全部详情配置
ipconfig /release 释放归还IP地址
ipconfig /renew 重新租用地址
```

```
netstat：显示与 IP、TCP、UDP 和 ICMP 协议相关的统计数据，用于检验本机各端口网络连接情况。
参数说明：
	-a 看全部
    -e 看以太网
    -n 数字显示地址加端口
    -r 路由表
    -s 协议统计信息
```

```
ssh：远程处理连接
ssh username@hostname	:username是SSH服务器的用户名，hostname是SSH服务器的主机名或IP地址
```

```
scp:远程文件传输
scp 文件路径名
```

